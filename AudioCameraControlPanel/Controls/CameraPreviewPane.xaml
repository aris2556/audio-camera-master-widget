<UserControl x:Class="AudioCameraControlPanel.Controls.CameraPreviewPane"
             x:Name="Root"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource PreviewSurfaceBrush}"
            BorderBrush="#202C38"
            BorderThickness="1"
            CornerRadius="8"
            MinHeight="{Binding PreviewMinHeight, ElementName=Root}"
            ClipToBounds="True">
        <Grid>
            <Grid.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#101820" Offset="0" />
                    <GradientStop Color="#172333" Offset="1" />
                </LinearGradientBrush>
            </Grid.Background>
            <TextBlock Text="Preview stopped"
                       Foreground="#A9B6C4"
                       FontSize="15"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       Visibility="{Binding IsStopped, ElementName=Root, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Image Source="{Binding Frame, ElementName=Root}"
                   Stretch="Uniform"
                   SnapsToDevicePixels="True" />
        </Grid>
    </Border>
</UserControl>
